import styles from './Path.module.scss';

export const Path = (): JSX.Element => (
  <section className={styles.section}>
    <div className={styles.path}>Products/</div>
  </section>
);
// Div will take a current path for some products in feature.
